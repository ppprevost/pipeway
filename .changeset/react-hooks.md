---
"pipeway-react": minor
---

New package — React hooks over `pipeway-client`: `useQuery` and `useMutation`,
Result-first (typed `{ data, error, status }`, no thrown promises), zero
dependencies beyond React.
